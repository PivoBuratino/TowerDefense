using UnityEngine;

public class LevelConditionScore : MonoBehaviour//, ILevelCondition
{
    /*
    [SerializeField] private int score;

    [SerializeField] private float bonusTime;
    public float BonusTime => bonusTime;

    [SerializeField] private float multiplyScore;
    public float MultiplyScore => multiplyScore;

    private bool m_Reached;
    bool ILevelCondition.IsCompleted
    {
        /*
        get
        {
            if(Player.Instance != null && Player.Instance.ActiveShip != null)
            {
                if(Player.Instance.Score >= score)
                {
                    m_Reached = true;                  
                }
            }
            return m_Reached;
        }
        
    }    
    */
}
